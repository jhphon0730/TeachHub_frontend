"use client"

import React from 'react'

import { Button } from "@/components/ui/button"

const CreateCourseModal = () => {
	return (
		<React.Fragment>
			<Button variant="outline" className="mb-4"> Create a new course</Button>

		</React.Fragment>
	)
}

export default CreateCourseModal;
